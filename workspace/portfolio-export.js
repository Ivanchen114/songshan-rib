export const escapeHtml=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function portfolioHtml({title,term,items,includeFeedback=false}){
 const e=escapeHtml;
 return `<!doctype html><html lang="zh-Hant"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data:; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'"><title>${e(title)}</title><style>body{max-width:860px;margin:50px auto;padding:0 24px;background:#faf8f1;color:#233c34;font:17px/1.8 system-ui,sans-serif}h1,h2{font-family:serif}h1{font-size:38px}article{border-top:1px solid #9ca79b;margin:40px 0;padding-top:24px}img{display:block;max-width:100%;max-height:850px;margin:20px auto;object-fit:contain}p{white-space:pre-wrap;overflow-wrap:anywhere}small{color:#52665d}section,figure{break-inside:avoid}@media print{body{margin:0;background:white;font-size:11pt}article{break-before:page}img{max-height:220mm}}</style><body><header><small>R.I.B. · ${e(term)} 學期 · 私人選錄</small><h1>${e(title||'我的閱讀與表達作品集')}</h1><p>由既有作品選錄，不新增作業或分數。小組作品是共同成果，不代表每位成員的個別能力。</p><p>本檔供本人及授課教師保存；${includeFeedback?'含同儕回饋，請勿直接公開。':'分享前仍須檢查圖片及文字中的個資。'}圖片已嵌入，可離線開啟；以瀏覽器「列印 → 另存 PDF」。</p></header>${items.map(({work,version,images})=>`<article><h2>${e(work.title)} · V${version.ordinal}</h2><small>${e(version.created_at)} · ${e(work.kind.includes('rebuild')||work.kind==='w5-workshop'||work.kind==='w15-deck'?'小組共同作品':'個人作品')}</small>${version.metadata.text?`<p>${e(version.metadata.text)}</p>`:''}${images.map((src,i)=>/^data:image\/jpeg;base64,[A-Za-z0-9+/=]+$/.test(src)?`<figure><img alt="作品第 ${i+1} 張" src="${src}"></figure>`:'').join('')}${work.decisions.filter(d=>d.version_id===version.id).map(d=>`<section><h3>我的${d.choice==='keep'?'保留':'修改'}與依據</h3><p>${e(d.reason)}</p></section>`).join('')}${includeFeedback?work.reviews.filter(r=>r.version_id===version.id).map(r=>`<section><h3>同學當時的理解</h3><p>${e(r.situation)}\n${e(r.meaning)}</p></section>`).join(''):''}<small>來源版本 ${e(version.id)}</small></article>`).join('')}<footer>這是下載當下的選錄快照，後續改動不會自動更新。</footer></body></html>`;
}
